---
name: checkpoint
description: Write a checkpoint now or show the current checkpoint
---

Run `cmh checkpoint now` using Bash to trigger an immediate checkpoint write using the configured writer model. Use `--dry-run` first if the user wants to preview. Use `cmh checkpoint show` to display the current checkpoint content.
